#include <stdio.h>
int main(int argc, char const *argv[])
{
    // Leitura do arquivo de entrada

    // FILE *file = fopen(argv[0], "r");

    printf("arg[1] = %s\n", argv[1]);
    printf("argv[2] = %s\n", argv[2]);

    // fclose(file);
    return 0;
}
